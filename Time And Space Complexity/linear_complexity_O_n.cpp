#include<bits/stdc++.h>

using namespace std;

int main(){

    // Big-O notation means linear complexity O(n)
    // Linear Complexity O(n)

    int n; // complexity O(1)
    cin >> n; // complexity O(1)

    for(int i = 1; i <= n; i++){ // complexity O(n
        // This loop runs n times, so the complexity is O(n)
        // Each iteration of the loop takes constant time O(1)
        // Therefore, the overall complexity of this loop is O(n)

        cout << i << " "; // complexity O(1
    }

    cout << endl; // complexity O(1)

    
    return 0 // complexity O(1);
}


// The overall time complexity of this code is O(n)
// always take the worst case scenario
// ignore constant factors
// so the time complexity is O(n)